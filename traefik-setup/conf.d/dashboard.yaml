# ==============================================
# DEBUG: Traefik Dashboard (No Auth)
# ==============================================

http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.starklab.mkroshana.com`)"
      entryPoints:
        - websecure
      service: api@internal
      # middlewares: []  <-- TEMPORARILY DISABLED
      tls:
        certResolver: cloudflare
